'use babel';

import AtomCodeClippingsView from '../lib/atom-code-clippings-view';

describe('AtomCodeClippingsView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
